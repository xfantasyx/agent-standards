# 回归验证规则

每次评审通过后、人工验收前，必须执行全量回归测试。

## 规则

| 规则 | 说明 |
|------|------|
| **全量执行** | 跑项目中所有已有的单元测试和集成测试，不允许跳过 |
| **零失败** | 任何一个测试失败即判定不通过，必须修复后重新走评审流程 |
| **不可禁用** | 不允许通过注释/跳过/删除测试来"通过"回归验证 |
| **新增测试不豁免** | 本次 Session 新写的测试也纳入回归，确保自身一致 |
